spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mt_project_tool?useSSL=false&serverTimezone=UTC
    username: root
    password: oronero
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  application:
    name: multi-tenant-project-tool

server:
  port: 8080
